export interface CharacterProps {
  id: number;
  characterId: number;
  name: string;
  phoneNumber: string;
}

export interface CharacterClassProps {
  source: number;
  name: string;
  characterId: number;
  phoneNumber: string;
}
