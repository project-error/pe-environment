export enum CharacterEvents {
  SEND_CHARACTERS = 'pe:character:sendCharacters',
  GET_CHARACTERS = 'pe:character:getCharacters',
  SELECT_CHARACTER = 'pe:characterSelected',
  CREATE_CHARACTER = 'pe:createCharacter',
}
